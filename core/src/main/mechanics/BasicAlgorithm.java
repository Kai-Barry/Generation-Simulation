package main.mechanics;

import main.utility.TileType;

public class BasicAlgorithm extends BaseTile {
    private BasicAlgorithm(TileType tileType, GridIndex gridIndex) {
        super(tileType, gridIndex);
    }
}
